# Configuration du serveur
NODE_ENV=development
PORT=3000
API_PREFIX=/api

# Configuration du logging
LOG_LEVEL=info
LOG_FORMAT=dev

# Configuration CORS
CORS_ORIGIN=*

# Configuration de la base de données
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pixelboard
DB_USER=postgres
DB_PASSWORD=postgres

# Configuration JWT
JWT_SECRET=replace-this-with-a-secure-secret-in-production
JWT_EXPIRES_IN=90d